name: CI/CD

on:
  push:
    branches:
      - main  # Deploy on push to main branch

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
      HEROKU_APP_NAME: ${{ secrets.HEROKU_APP_NAME }}
      HEROKU_EMAIL: ${{ secrets.HEROKU_EMAIL }}

    steps:
      # Step 1: Checkout the repository
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0  # Fetch all history for all branches and tags

      # Step 2: Set up Python
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version-file: ".python-version"

      # Step 3: Install uv
      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          version: "0.9.25"

      # Step 4: Install dependencies
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libxml2-dev libxslt1-dev python3-dev

      - name: Install dependencies
        run: |
          uv python install "$(cat .python-version)"
          uv venv --python "$(cat .python-version)"
          uv pip install -r requirements.txt

      - name: Activate venv
        run: |
          source .venv/bin/activate

      # Step 5: Set environment variables (including OpenAI API key and Flask secret key)
      - name: Set environment variables
        run: |
          echo "OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}" >> $GITHUB_ENV
          echo "FLASK_SECRET_KEY=${{ secrets.FLASK_SECRET_KEY }}" >> $GITHUB_ENV

      # TODO: Insert tests here

      # Step 6: Install Heroku CLI
      - name: Install Heroku CLI
        if: ${{ vars.DEPLOY_HEROKU == 'true' && env.HEROKU_API_KEY != '' && env.HEROKU_APP_NAME != '' && env.HEROKU_EMAIL != '' }}
        run: |
          curl https://cli-assets.heroku.com/install.sh | sh

      # Step 7: Deploy to Heroku
      - name: Deploy to Heroku
        if: ${{ vars.DEPLOY_HEROKU == 'true' && env.HEROKU_API_KEY != '' && env.HEROKU_APP_NAME != '' && env.HEROKU_EMAIL != '' }}
        uses: akhileshns/heroku-deploy@v3.13.15
        with:
          heroku_api_key: ${{ env.HEROKU_API_KEY }}
          heroku_app_name: ${{ env.HEROKU_APP_NAME }}
          heroku_email: ${{ env.HEROKU_EMAIL }}
